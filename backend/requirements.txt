# ── Framework ─────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# ── Base de données ───────────────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# ── Auth / Sécurité ───────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.27.0

# ── IPFS ──────────────────────────────────────────────────────────────────────
ipfshttpclient==0.8.0a2

# ── QR Code ▶ NOUVEAU v6 ──────────────────────────────────────────────────────
qrcode[pil]==7.4.2
Pillow==10.3.0

# ── PDF generation microservice
# original locked to 3.7.0 which isn't on PyPI for Python 3.12;
# allow any compatible 3.x release that exists
reportlab>=3.5,<4.5

# ── Scheduler Retry Worker ▶ NOUVEAU v6 ───────────────────────────────────────
apscheduler==3.10.4

# ── Retry / Circuit-breaker ▶ NOUVEAU v6 ─────────────────────────────────────
tenacity==8.3.0

# ── Email async ▶ NOUVEAU v6 ─────────────────────────────────────────────────
aiosmtplib==3.0.1
jinja2==3.1.4

# ── Logs structurés ▶ NOUVEAU v6 ─────────────────────────────────────────────
structlog==24.1.0
